// DIAGNOSTIC FIXTURE - baseline for the ballast sweep (zero added footprint).
// Identical to deep_negative_shops; exists so every ballast case compares
// against a case built through the same include path.
#define DIAG_BALLAST_KB 0
#include "filters/deep_negative_shops.cl"
